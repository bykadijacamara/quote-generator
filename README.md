# Quote Generator

A simple quote generator that fetches random quotes from an API.

## Features
- Fetches quotes using the Quotable API
- Displays the quote and author
- Handles errors gracefully

## How to Use
Click the "Generate Quote" button to get a new quote.

## Live Demo
(You can add a live link here later if you deploy it!)